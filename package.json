{
  "name": "juniper",
  "version": "0.1.0",
  "private": true,
  "description": "AI-native elderly patient management platform on Medplum: agentic 4M voice check-ins, caregiver monitoring, one-time onboarding.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test:voice": "cd services/voice && ../../.venv/bin/python -m pytest"
  },
  "engines": {
    "node": ">=20"
  }
}
